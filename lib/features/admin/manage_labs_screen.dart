import 'package:flutter/material.dart';

class ManageLabsScreen extends StatelessWidget {
  const ManageLabsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Manage Labs'),
      ),
      body: const Center(
        child: Text('Manage Labs Screen - Coming Soon'),
      ),
    );
  }
}